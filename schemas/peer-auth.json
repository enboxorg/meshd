{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://enbox.org/schemas/wireguard-mesh/peer-auth",
  "type": "object",
  "required": ["networkDid", "networkRecordId", "authorizedAt"],
  "additionalProperties": false,
  "properties": {
    "networkDid": {
      "type": "string",
      "description": "DID of the network owner (anchor DWN tenant)"
    },
    "networkRecordId": {
      "type": "string",
      "description": "Record ID of the network record on the anchor DWN"
    },
    "authorizedAt": {
      "type": "string",
      "format": "date-time",
      "description": "When this peer was authorized to read node data"
    }
  }
}
