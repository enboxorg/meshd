name: Release Please

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    outputs:
      release_created: ${{ steps.release.outputs.release_created }}
      tag_name: ${{ steps.release.outputs.tag_name }}
    steps:
      - uses: googleapis/release-please-action@v4
        id: release
        with:
          # release-type + always-bump-patch versioning live in
          # release-please-config.json (manifest mode); the current version is
          # tracked in .release-please-manifest.json. This hard-enforces the
          # patch-only policy so feat: commits can't drive a minor bump.
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json
          token: ${{ secrets.GITHUB_TOKEN }}

  build-artifacts:
    name: Build ${{ matrix.os }}-${{ matrix.arch }}
    needs: release-please
    if: needs.release-please.outputs.release_created == 'true'
    # darwin targets build on a macOS runner so the Mach-O binaries are ad-hoc
    # code-signed; Apple Silicon SIGKILLs unsigned binaries. Everything else
    # cross-compiles on Linux.
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: linux
            arch: x64
            goos: linux
            goarch: amd64
            ext: tar.gz
            runner: ubuntu-latest
          - os: linux
            arch: arm64
            goos: linux
            goarch: arm64
            ext: tar.gz
            runner: ubuntu-latest
          - os: darwin
            arch: x64
            goos: darwin
            goarch: amd64
            ext: tar.gz
            runner: macos-latest
          - os: darwin
            arch: arm64
            goos: darwin
            goarch: arm64
            ext: tar.gz
            runner: macos-latest
          - os: windows
            arch: x64
            goos: windows
            goarch: amd64
            ext: zip
            runner: ubuntu-latest
          - os: windows
            arch: arm64
            goos: windows
            goarch: arm64
            ext: zip
            runner: ubuntu-latest
          - os: freebsd
            arch: x64
            goos: freebsd
            goarch: amd64
            ext: tar.gz
            runner: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.release-please.outputs.tag_name }}

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.x'

      - name: Build binary
        env:
          CGO_ENABLED: '0'
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          mkdir -p dist/release
          out='dist/release/meshd'
          if [ "${{ matrix.os }}" = 'windows' ]; then
            out='dist/release/meshd.exe'
          fi
          version="${{ needs.release-please.outputs.tag_name }}"
          version="${version#v}"
          go build -trimpath -ldflags="-s -w -X main.version=${version}" -o "$out" ./cmd/meshd

      - name: Ad-hoc code-sign (macOS)
        if: matrix.os == 'darwin'
        run: |
          codesign --force --sign - dist/release/meshd
          codesign --verify --verbose dist/release/meshd

      - name: Package artifact
        run: |
          artifact="meshd-${{ matrix.os }}-${{ matrix.arch }}.${{ matrix.ext }}"
          if [ "${{ matrix.ext }}" = 'zip' ]; then
            (cd dist/release && zip -q "../../${artifact}" meshd.exe)
          else
            (cd dist/release && tar -czf "../../${artifact}" meshd)
          fi
          echo "artifact=${artifact}" >> "$GITHUB_ENV"

      - name: Upload artifact to release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.release-please.outputs.tag_name }}
          files: ${{ env.artifact }}

  install-smoke:
    name: Smoke test public installer
    needs:
      - release-please
      - build-artifacts
    if: needs.release-please.outputs.release_created == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.release-please.outputs.tag_name }}

      - name: Install latest release via meshd.sh
        env:
          MESHD_INSTALL_EXPECTED_VERSION: ${{ needs.release-please.outputs.tag_name }}
          MESHD_INSTALL_FALLBACK_URL: https://raw.githubusercontent.com/${{ github.repository }}/${{ needs.release-please.outputs.tag_name }}/install.sh
        run: ./scripts/e2e/install-smoke.sh
